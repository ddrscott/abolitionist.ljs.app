# Work Queue

- [x] Document architecture conversation (extraction → Fumadocs → static search → Cloudflare RAG path) — see `.claude/work-queue/architecture-conversation-doc.md`
- [x] Initialize git, write a reasonable `.gitignore`, and commit the current state
